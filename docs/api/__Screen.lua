---@class Screen:Viewable,Display
local M = {}

---返回当前是否保持虚拟屏幕的位图不变
---@return boolean 保持屏幕的状态
function M:isKeepDisplay()
    
end

---是否保持当前虚拟屏幕的位图不变
---@param is boolean true则保持不变
function M:keepDisplay(is)
    
end

---先刷新一下位图内的屏幕数据，然后保持不变
function M:updateAndKeepDisplay()
    
end